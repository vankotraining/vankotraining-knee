name: Tindeq Repeaters CI

on:
  push:
    branches:
      - feature/tindeq-repeaters-import
  pull_request:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci
      - run: npm audit --omit=dev --audit-level=high
      - run: npm test
      - run: npm run check:types

      - name: Verify no new lint findings
        shell: bash
        run: |
          git worktree add /tmp/knee-main origin/main
          cd /tmp/knee-main
          npm install --silent
          set +e
          npm run lint > /tmp/lint-main.log 2>&1
          set -e
          cd "$GITHUB_WORKSPACE"
          set +e
          npm run lint > /tmp/lint-feature.log 2>&1
          set -e
          python - <<'PY'
          import collections
          import re
          from pathlib import Path

          issue_start = re.compile(r"^\s*\d+:\d+\s+(error|warning)\s+")
          rule_pattern = re.compile(r"\s{2,}(@?[\w-]+(?:/[\w-]+)+)\s*$")

          def parse(path):
              current = None
              severity = None
              issues = []
              for line in Path(path).read_text(encoding="utf-8").splitlines():
                  stripped = line.strip()
                  if stripped.startswith("/") and stripped.endswith((".ts", ".tsx", ".js", ".jsx")):
                      current = "src/" + stripped.split("/src/", 1)[1] if "/src/" in stripped else stripped
                      severity = None
                      continue
                  start = issue_start.match(line)
                  if start:
                      severity = start.group(1)
                  rule = rule_pattern.search(line)
                  if current and severity and rule:
                      issues.append((current, severity, rule.group(1)))
                      severity = None
              return collections.Counter(issues)

          added = parse("/tmp/lint-feature.log") - parse("/tmp/lint-main.log")
          if added:
              raise SystemExit(
                  "Feature branch introduced new lint findings:\n"
                  + "\n".join(f"{item}: {count}" for item, count in sorted(added.items()))
                  + "\n\n"
                  + Path("/tmp/lint-feature.log").read_text(encoding="utf-8")
              )
          print("Lint baseline preserved.")
          PY

      - run: npm run build

  database-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v2
        with:
          version: 2.101.0
          github-token: ${{ github.token }}

      - name: Start isolated Supabase database
        run: |
          mkdir -p /tmp/tindeq-supabase
          cd /tmp/tindeq-supabase
          supabase init
          supabase db start

      - name: Apply baseline and Tindeq migration
        env:
          PGPASSWORD: postgres
        run: |
          psql \
            --host 127.0.0.1 \
            --port 54322 \
            --username postgres \
            --dbname postgres \
            --set ON_ERROR_STOP=1 \
            --file supabase/tests/tindeq_ci_baseline.sql \
            --file supabase/migrations/20260801_tindeq_repeaters_stage1.sql

      - name: Verify schema, RLS and private Storage
        env:
          PGPASSWORD: postgres
        run: |
          psql \
            --host 127.0.0.1 \
            --port 54322 \
            --username postgres \
            --dbname postgres \
            --set ON_ERROR_STOP=1 \
            --file supabase/tests/tindeq_stage1_integration.sql

      - name: Stop isolated Supabase database
        if: always()
        run: |
          cd /tmp/tindeq-supabase
          supabase stop --no-backup
